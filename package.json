{
	"name": "capacity-tasks",
	"version": "0.1.0",
	"description": "Obsidian plugin: capacity-gated task filtering. Working name, pre-release.",
	"main": "main.js",
	"type": "module",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"check": "biome check src"
	},
	"keywords": [],
	"author": "tanya-ok",
	"license": "MIT",
	"devDependencies": {
		"@biomejs/biome": "^2.5.2",
		"@types/node": "^24.0.0",
		"builtin-modules": "^4.0.0",
		"esbuild": "^0.28.1",
		"obsidian": "latest",
		"tslib": "^2.8.0",
		"typescript": "^5.7.0"
	}
}
